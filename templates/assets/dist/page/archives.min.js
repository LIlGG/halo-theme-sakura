import { d as documentFunction } from "../main.min.js";
import "../assets/i18next-02f7db4d.js";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __decorateClass = (decorators, target, key, kind) => {
  var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc(target, key) : target;
  for (var i = decorators.length - 1, decorator; i >= 0; i--)
    if (decorator = decorators[i])
      result = (kind ? decorator(target, key, result) : decorator(result)) || result;
  if (kind && result)
    __defProp(target, key, result);
  return result;
};
class Archives {
  registerArchiveEvent() {
    const archiveTitleElements = document.querySelectorAll(".archives-article .archive-title h3");
    archiveTitleElements.forEach((element) => {
      element.addEventListener("click", () => {
        var _a;
        const archiveElement = (_a = element.parentElement) == null ? void 0 : _a.parentElement;
        const posts = archiveElement == null ? void 0 : archiveElement.querySelector(".archive-posts");
        if (archiveElement == null ? void 0 : archiveElement.classList.contains("active")) {
          posts.style.maxHeight = "0";
          archiveElement.classList.remove("active");
        } else {
          var height = posts == null ? void 0 : posts.scrollHeight;
          archiveElement == null ? void 0 : archiveElement.classList.add("active");
          posts.style.maxHeight = height + "px";
        }
      });
    });
  }
}
__decorateClass([
  documentFunction(false)
], Archives.prototype, "registerArchiveEvent", 1);
